# Claims registry

Every public statement IntentLane makes, the level it belongs to in
[COMMERCIAL-READINESS.md](COMMERCIAL-READINESS.md), the platform and version it
names, the evidence behind it and its status. A claim is only publishable at or
below its status.

## Registry

| Claim | Level | Platform and version | Evidence | Status |
| --- | --- | --- | --- | --- |
| `intentlane audit` reports, read only, what a repository implements, what the target platform ships and what is feasible. | Auditor | macOS 27 and iOS 27 | `intentlane audit` run on `apps/example-macos` (shelf, studio, reader), on `apps/example-expo` and on the NetNewsWire fork, with the worktree fingerprint unchanged; 277 tests. | Proven |
| The audit separates schema-backed readiness from shortcut-only readiness. | Auditor | macOS 27 | The `discovery` field (`schema-backed`, `shortcuts-only`, `none`) plus `ILA140`; the NetNewsWire fork moves from `shortcuts-only` to `schema-backed`. | Proven |
| The audit scopes every capability to the Xcode targets that compile for the requested platform. | Auditor | macOS 27 and iOS 27 | `targets 8 target(s) (3 for macos)` and `(4 for ios)` on NetNewsWire, whose macOS report is `0/100 (none, none)` while the iOS report keeps three implemented capabilities. | Proven |
| A contract compiles to App Intents that build for macOS 27 and iOS 27. | Build | macOS 27 and iOS 27 | The generated Swift compiles for `arm64-apple-macos27.0` and for `arm64-apple-ios27.0`, and `appintentsmetadataprocessor` extracts the metadata on both. | Proven |
| A conformed intent registers an App Schema and its system protocol. | Build | macOS 27 and iOS 27 | `OpenArticle` carries `AssistantIntent` and `OpenEntity` with `ReaderOpenPageIntent`; `RotatePages` carries `ReaderRotatePagesIntent`; `IntentLaneArticleEntity` carries `ReaderPageEntity`. | Proven |
| A conformed entity that is the target of an action is resolvable, so the system can open or delete it. | Build | macOS 27 and iOS 27 | The generator adds `IndexedEntity` and `import CoreSpotlight`; the metadata processor refuses the schema without it. | Proven |
| The generated code targets Swift 6 concurrency checking. | Build | macOS 27 (Swift 6.2 target) | Two real defects were found by the pilot and fixed: a `static var` query and non-`Sendable` protocols, then the missing initializer on a conformed entity. The fork builds clean. | Proven |
| Actions generated by IntentLane appear in the Shortcuts app. | Surfaces | macOS 27 | None. The `shortcuts` command lists user shortcuts, not the App Shortcuts an application provides. | Awaiting proof |
| Indexed content generated by IntentLane is found in Spotlight. | Surfaces | macOS 27 | None. No indexing lifecycle was implemented or observed. | Awaiting proof |
| A named Siri journey works on macOS 27. | Siri journey | macOS 27 | None. No process can drive Siri, so the manual positive and negative cases remain open. | Awaiting proof |
| A named domain package is supported end to end. | Domain package | macOS 27 and iOS 27 | The `reader` domain is complete: three schemas proven on both platforms and the versioned fixture `apps/example-macos/reader.yaml`. | Partially proven, one pilot where the rule asks for two independent pilots |

## How to read this

`Proven` means a command, a file or an extracted metadata field shows it, and the
claim may be repeated. Some rows rest on the NetNewsWire pilot, whose fork and
evidence ledger live outside this repository and have never been reproduced by
a second person; those rows name the fork explicitly and may be repeated only
as the metadata and audit facts they describe. `Partially proven` means the
technical result holds but a stated condition of the level does not, so the
claim has to name the gap. `Awaiting proof` means the claim may not be published
at all, and the reason it is missing is recorded next to it.

No row names a platform without a version, and no row claims live Siri
behaviour from metadata, because metadata proves registration and never
resolution or language execution.

## What a new claim needs

1. A named platform and version, not a year and not a previous generation.
2. A cited proof: a command, a file, a test or an extracted metadata field.
3. A level from the claims matrix that the proof actually reaches.
4. A status, and when the status is not `Proven`, the reason and what would
   close it.
